"use strict" //It will make our compiler to treat my code as newer version

//alert (3+3) // we are using nodejs not browser

let name = "Amrit"
let age = 20
let isLoggedIn = false


// Primitive data types
//number
//string
//null
//boolean
//bigint
//undefined
//symbol - > uniqueness

console.log(typeof(age)) // these are the types like number, boolean, string, undefined
console.log(typeof(isLoggedIn))
console.log(typeof(null)) // but null is not type it is object